export type SubmissionPayload = {
    code: string; // The code to be executed    
    language: string; // The programming language of the code
    testCases: string; // test cases to validate the code
}